export interface Audiodata {
    id: Number,
    created: String,
    creator: Number,
    title: String,
    description: String,
    downvotes: Number,
    upvotes: Number,
    tag: String,
    visibility: Boolean
}